class Solution:
    def missingNumber(self,arr):
        positive_nums = [num for num in arr if num > 0]
        num_set = set(positive_nums)
        i = 1
        while i in num_set:
            i += 1
        return i
