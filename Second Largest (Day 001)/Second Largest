class Solution:
    def getSecondLargest(self, arr):
        if len(set(arr))<2:
            return -1
        
        sor_array = sorted(set(arr), reverse = True)
        
        return sor_array[1]
